services:
  backend:
    build:
      context: ./backend
    environment:
      APP_ENV: production
      DATABASE_URL: sqlite:///./data/trendscope.db
      FRONTEND_ORIGIN: http://localhost:5081
      PROVIDER_MODE: mock
    ports:
      - "5081:5081"
    volumes:
      - backend-data:/app/data

volumes:
  backend-data:
