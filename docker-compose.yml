services:
  backend:
    build:
      context: ./backend
    environment:
      APP_ENV: production
      DATABASE_URL: sqlite:///./data/trendscope.db
      FRONTEND_ORIGIN: http://localhost:5060
      PROVIDER_MODE: mock
    ports:
      - "5060:5060"
    volumes:
      - backend-data:/app/data

volumes:
  backend-data:
